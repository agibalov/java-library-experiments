# spring-jmh-experiment

* `./gradlew clean test` - run benchmark tests (benchmark + result verification)
* `./gradlew clean benchmark` - run all benchmarks without verifications, put results under `build/jmh`
